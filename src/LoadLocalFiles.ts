import Config from './config.json';

class LoadLocalFiles {
  constructor(database: any) {
            
      
  }
  
  loadQueries() {
      
  }
  
  //SELECT * FROM files ORDER BY ID DESC LIMIT 5
  
}

export default LoadLocalFiles;
